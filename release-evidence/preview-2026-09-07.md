# Continuous-panorama preview verification — 7 September 2026

This records technical preview checks, not permission, editorial approval, or production readiness.

## Exact candidate

- Commit: `d852d3aee32699b4f168f36486a4af6d76bf82de` on `preview/initial-review`.
- [Vercel Preview](https://bayeux-tapestry-explorer-a8qt2xgb8-fhoskyns-projects.vercel.app/?scene=01), deployment `dpl_74AuLjzAEHNEnvvcgJtkHSPbAcnF`.
- Vercel reports target `preview`, status `Ready`; build logs confirm the candidate commit and `Production release gate skipped for preview build`.
- [GitHub validation run](https://github.com/fhoskyns/bayeux-tapestry-explorer/actions/runs/34158562029): application and tile-infrastructure jobs both succeeded for this exact commit.
- `VITE_TAPESTRY_TILE_BASE_URL` is public configuration scoped only to Preview branch `preview/initial-review`. A Production environment listing remains empty; no production promotion was performed.

## Asset delivery

- [Full local verification](./deepzoom-v1-verification.json): 3,899 lossless tiles, 6,816 seams and all 2,826 native tiles checked against the locked master.
- [Exhaustive hosted verification](./deepzoom-v1-remote-verification.json): every tile and the DZI descriptor fetched through the read-only Worker, with matching SHA-256, byte length, MIME, CORS and immutable caching.
- Worker smoke checks: DZI GET/HEAD 200, matching conditional request 304, disallowed POST 405, query-bearing/private/missing paths 404, representative far-edge tile 200. TLS verification remained enabled.
- The browser viewer opens the DZI, not the multi-gigabyte master. Its navigator uses a resized Commons thumbnail; the private master is never a viewer source.
- The separate private master archive passed a complete ranged read-back SHA-256 check; see the [archive report](./master-v1-archive-verification.json). The temporary authenticated transfer connection was disposed after verification.

## Application and browser

- Local `pnpm check` with the hosted tile origin: structural content validation, TypeScript, lint, all 47 tests, static build and rendered-route verification passed.
- Protected HTTP smoke checks against `/`, `/sources`, and `/?scene=invalid&annotation=missing` returned 200 HTML without disabling deployment protection.
- Deployed desktop: the tapestry fills the viewport; dragging crosses scene boundaries continuously, the settled scene/URL updates, and vertical dragging leaves `y=0` while the full height is visible. Browser error log was empty after that interaction.
- Deployed mobile at 390 × 844: canvas fills the viewport and document width stays 390; Scene 32 loads, tapping an annotation pins its sourced note, Escape closes it and restores marker focus. The temporary viewport override was reset afterwards.
- Additional local checks using the same hosted panorama: overview/Start, Scene 58 and disabled final Next, navigator End, detail zoom, full-height vertical lock and mobile controls.
- Regression tests cover mounted URL restoration without reopening the source, settled free-pan scene selection, stale-note clearing, retry preserving the current viewport, and double-click/double-tap zoom entering free exploration.

## Remaining publication gates

All 58 scenes and 116 annotations remain explicit editorial drafts. Scene bounds are proportional estimates and hotspot selectors are placement hints, not calibrated coordinates. Scholarly review, coordinate calibration and a documented production-publication basis remain required. The production release gate remains intentionally blocked.
